# Server
PORT=5000
NODE_ENV=development

# Rate Limiting
# Set to 'false' to disable rate limiting globally (default: true)
RATE_LIMITER_ENABLED=true

# Socket.IO Debug Logging
# Set to 'true' to enable detailed socket event logging (default: false)
# Warning: Enabling this in production will impact performance
SOCKET_DEBUG_LOGGING=false

# Kafka Configuration (Optional)
# Set to 'true' to enable Kafka for message queue processing (default: false)
# When disabled, messages are processed directly through Socket.IO
# Kafka provides better handling of consecutive messages and horizontal scaling
ENABLE_KAFKA=false
KAFKA_BROKER=localhost:9092

# Database
MONGODB_URI=localhost:27017

# Frontend URL (used for push notification redirect links, email links, etc.)
# In production: Set this to your production frontend URL (e.g., https://fieldsy.co.uk)
FRONTEND_URL=http://localhost:3000

# Production URLs (for CORS and fallback)
# If FRONTEND_URL is not set, PRODUCTION_FRONTEND_URL is used as fallback
PRODUCTION_FRONTEND_URL=https://fieldsy.indiitserver.in
PRODUCTION_ADMIN_URL=https://fieldsy-admin.indiitserver.in

# JWT Secret
JWT_SECRET=your-jwt-secret-key-here

# Email Configuration (Nodemailer)
# For Gmail: smtp.gmail.com, port: 587
# For Outlook: smtp-mail.outlook.com, port: 587
# For Yahoo: smtp.mail.yahoo.com, port: 587
# For Custom SMTP: your-smtp-server.com, port: 587 or 465
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=deependraindiit@gmail.com
SMTP_PASS=twuk gcbh mlvn zunn
# SMTP_USER=veninderindiit@gmail.com
# SMTP_PASS=iath chdk lbuq tirl 
EMAIL_FROM="Fieldsy <noreply@fieldsy.com>"
EMAIL_FROM_NAME=Fieldsy

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Stripe Webhook Secrets
# Main webhook for platform account events (payments, refunds on your account)
# Dashboard: Developers > Webhooks > "Events on your account"
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Connect webhook for connected account events (payouts to field owners)
# Dashboard: Developers > Webhooks > "Events on Connected accounts"
STRIPE_CONNECT_WEBHOOK_SECRET=whsec_xxx

# Optional: Dedicated webhook secrets for separate endpoints
# If you create separate webhook endpoints for refunds and payouts
# Dashboard: Create separate endpoints for /api/webhooks/refunds and /api/webhooks/payouts
STRIPE_REFUND_WEBHOOK_SECRET=whsec_xxx
STRIPE_PAYOUT_WEBHOOK_SECRET=whsec_xxx

# Payout Engine Toggle
# Set to 'true' to use @fieldsy/stripe-auto-payout engine for payouts,
# webhooks, and scheduled jobs. Set to 'false' or leave unset to use
# the built-in payout services.
USE_PAYOUT_ENGINE=true

# Google Sign In (for server-side token verification)
# Get these from Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# Optional: Different client IDs for iOS and Android apps
GOOGLE_IOS_CLIENT_ID=
GOOGLE_ANDROID_CLIENT_ID=

# AWS S3
S3_BASE_URL=https://fieldsy.s3.us-east-1.amazonaws.com
S3_BUCKET_NAME=fieldsy
S3_REGION=us-east-1